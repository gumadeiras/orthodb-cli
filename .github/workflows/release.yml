name: release

on:
  push:
    tags:
      - "v*"
  workflow_dispatch:

jobs:
  build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v6
      - uses: actions/setup-python@v6
        with:
          python-version: "3.13"
      - name: Install release tooling
        run: python -m pip install -e '.[release]'
      - name: Run tests
        run: PYTHONPATH=src python -m unittest discover -s tests
      - name: Build package
        run: python -m build
      - name: Validate distributions
        run: python -m twine check dist/*
      - name: Upload artifacts
        uses: actions/upload-artifact@v4
        with:
          name: dist
          path: dist/*

  github-release:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: write

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Publish GitHub release
        uses: softprops/action-gh-release@v3
        with:
          files: dist/*
          generate_release_notes: true

  pypi:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      id-token: write

    environment:
      name: pypi

    steps:
      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Publish to PyPI
        uses: pypa/gh-action-pypi-publish@release/v1

  homebrew-tap:
    needs:
      - build
      - github-release
      - pypi
    runs-on: ubuntu-latest
    if: startsWith(github.ref, 'refs/tags/v')
    env:
      TAP_TOKEN: ${{ secrets.HOMEBREW_TAP_TOKEN }}

    steps:
      - name: Check tap token
        run: |
          if [ -z "$TAP_TOKEN" ]; then
            echo "missing HOMEBREW_TAP_TOKEN secret" >&2
            exit 1
          fi

      - name: Download dist artifacts
        uses: actions/download-artifact@v4
        with:
          name: dist
          path: dist

      - name: Checkout Homebrew tap
        uses: actions/checkout@v6
        with:
          repository: gumadeiras/homebrew-tap
          token: ${{ env.TAP_TOKEN }}
          path: homebrew-tap

      - name: Update formula
        run: |
          version="${GITHUB_REF_NAME#v}"
          sdist="dist/orthodb-${version}.tar.gz"
          sha256="$(sha256sum "$sdist" | awk '{print $1}')"
          python homebrew-tap/scripts/update_formula.py \
            --formula homebrew-tap/Formula/orthodb.rb \
            --url "https://github.com/gumadeiras/orthodb-cli/releases/download/v${version}/orthodb-${version}.tar.gz" \
            --sha256 "$sha256"

      - name: Commit and push formula
        working-directory: homebrew-tap
        run: |
          if git diff --quiet -- Formula/orthodb.rb; then
            echo "Formula/orthodb.rb already up to date"
            exit 0
          fi

          git config user.name "github-actions[bot]"
          git config user.email "41898282+github-actions[bot]@users.noreply.github.com"
          git add Formula/orthodb.rb
          git commit -m "Update orthodb to ${GITHUB_REF_NAME#v}"
          git push
